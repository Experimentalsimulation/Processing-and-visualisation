function [prms] = Get_prms(MIC)
    

             
        f = MIC.f;
        PPX = MIC.PXX;

        aPPX = mean([PPX(:, 1:3), 2); % Calculate the mean along the rows (dimension 2) 1:3 means only mic 1 to 3 are used
        df = f(2) - f(1);

        prms= sum(aPPX.*df);
             

end